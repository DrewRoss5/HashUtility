# HashUtility
A SHA256 Hashing Utility made to learn Tkinter

## How To Use:
To run this application on Windows, follow these steps:
- Ensure you have python installed, and that `pythonw` is added to your PATH
- Clone this repo
- Run the `run.bat` batch script
